<h1>Get ya jobs, come right up</h1>

  <div>
    <% if current_user %>
      <%= "Welcome #{current_user.name}" %>
    <% else %>
      <%= link_to 'Sign in with GitHub', login_path %>
    <% end %>
  </div>
  <div>
    <%= link_to 'Sign out!', signout_path %>
  </div>
